# Patch Package Notes

This project currently does not ship any patch-package overrides. Previous
overrides for older `@polymarket/clob-client` releases were removed after the
dependency upgrade, so `patch-package` runs with an empty patches directory.
